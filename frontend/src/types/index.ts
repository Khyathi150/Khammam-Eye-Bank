// Shared TypeScript types used across the app.
// Component-local types live alongside their components; put cross-cutting
// domain types (e.g. API response shapes) here as the project grows.
export {};
